name: gameplay
summary: Example caravan manifest for gameplay-related convoys
connectors:
  - connector: ExampleGameplayInputConvoy
README: |
  Minimal caravan manifest example.
  
  Repo-specific caravan manifests (fixtures) should live in the repo that consumes
  them (typically under `tests/Caravans/`).
